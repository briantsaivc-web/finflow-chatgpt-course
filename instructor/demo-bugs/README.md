# 現場故障示範檔

依序使用，不要先告訴學員根因：

1. `01-nan.html`：輸入空白後出現 `NaN`。
2. `02-double-click.html`：快速連點造成重複扣款。
3. `03-npc-stuck.html`：NPC 持續重試不可能完成的購買。
4. `04-stale-version.html`：畫面版本與程式版本標示不一致。

學員先用「操作、預期、實際、證據」回報，再使用 P08 最小修正卡。每個案例只修一個根因，修完要說明如何防止復發。
